.show_categories
  =t'Cookbook'
.show_dishes
  .show_dish
    - unless @dish.category.blank?
      = link_to @dish.category.name, admin_category_path(@dish.category)
      &#047
    - else       
      .lack=t'No Category'  
    %b= @dish.name
    %p= @dish.recipe
    
      
    = link_to (t'Edit'), edit_admin_dish_path(@dish)
    = link_to (t'Back'), admin_dishes_path
